Feature: Require tasks
  In order to reuse an existing tasks
  As a user
  I want to require tasks in a simple way

	#   Scenario: Morpheus should require "tasks" folder as defaults
	# Given a directory named "tasks"
	#     And a file named "tasks/alice.tasks" with:
	#     """
	#     :alice.tasks do
	#        task :welcome do
	#          say("Welcome, Alice")
	#        end
	#     end
	#     """
	#     When I run `task alice:welcome`
	#     Then the stdout should contain exactly:
	#     """
	#     [namespace: AliceTasks]: invoke :welcome
	#     Welcome, Alice
	# 
	#     """
  
